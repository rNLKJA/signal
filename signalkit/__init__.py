"""Signal: an interactive US public-data product with an LLM analyst layer
and an APS / EU AI Act aligned governance decision log.

The package is named ``signalkit`` (not ``signal``) so it never shadows
Python's standard-library ``signal`` module.

See ``signalkit.governance.decision_log`` for the audit-trail core and
``signalkit.analyst.core`` for the analyst layer that writes to it.
"""

__version__ = "0.14.0"
